# Configura��o do banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/cadastro?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=2006vgbr
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configura��o do JPA (Hibernate)
spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.ddl-auto=update # Pode ser "update", "create", "validate" ou "none"
spring.jpa.show-sql=true # Exibe as queries SQL no console para depura��o
